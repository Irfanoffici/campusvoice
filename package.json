{
  "name": "campus-feedback-deploy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../admin && npm install && npm run build && cd .. && node scripts/copy-admin.js"
  },
  "devDependencies": {
    "fs-extra": "^11.1.1"
  }
}